<resources>
    <style name="Base.Theme.ShopixAdmin" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/admin_primary</item>
        <item name="colorSecondary">@color/admin_accent</item>

        <!-- ✅ Fix: status bar should be solid to avoid camera cutout overlap -->
        <item name="android:statusBarColor">@color/admin_bg</item>
        <item name="android:navigationBarColor">@color/admin_bg</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@color/admin_bg</item>
        <!-- ✅ Enable fitsSystemWindows to push content below status bar -->
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Theme.ShopixAdmin" parent="Base.Theme.ShopixAdmin" />

    <!-- Navigation Menu Style -->
    <style name="NavMenuItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">32dp</item>
        <item name="android:paddingRight">32dp</item>
        <item name="android:paddingStart">32dp</item>
        <item name="android:paddingEnd">32dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/admin_text_primary</item>
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>
</resources>